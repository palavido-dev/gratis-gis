## What

What this PR changes, in a sentence or two.

## Why

The problem it solves or the issue it closes (link it: `Closes #123`).

## How tested

`pnpm typecheck` / `pnpm lint` / `pnpm test` results, plus any manual
steps you ran (which pages, which data).

## Docs

- [ ] Docs updated where behavior changed (or nothing user-facing changed)
